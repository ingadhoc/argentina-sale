<?xml version="1.0"?>
<odoo>
    <record id="sot_sale_order_type_form_view" model="ir.ui.view">
        <field name="name">sale.order.type.form</field>
        <field name="model">sale.order.type</field>
        <field name="inherit_id" ref="sale_order_type.sot_sale_order_type_form_view"/>
        <field name="arch" type="xml">
            <field name="sequence_id" position="after">
                <field name="discriminate_taxes" invisible="'AR'not in fiscal_country_codes"/>
                <field name="report_partner_id" invisible="'AR'not in fiscal_country_codes"/>
            </field>
        </field>
    </record>
</odoo>
